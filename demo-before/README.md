# 教学项目：补齐机制前

`pending_tasks()` 从任务列表生成待办视图，保留原顺序，复制任务记录。已有三个测试：顺序与过滤、视图修改不影响源记录、全部已完成时返回空列表。

运行 `python check.py`。该入口刻意保留一个缺口：它执行测试后没有把失败状态返回给调用方。因此屏幕出现测试失败时，进程仍可能以 0 结束。

本目录的正常业务代码可以通过测试。负例只由仓库根目录的演示脚本在临时副本中注入。
