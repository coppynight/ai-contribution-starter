# 教学项目：补齐机制后

产品范围、业务代码与三个测试均和 `demo-before/` 一致。`check.py` 捕获测试进程结果，再通过 `sys.exit(result.returncode)` 把结果交给调用方。

运行 `python check.py`。测试失败时入口也失败，供本地工具或 CI 识别；测试通过时返回 0。

后续贡献者交付修改理由、影响范围和实际检查结果。涉及改变业务范围时先由维护者决定；本示例没有配置权限或分支保护。发现回归时优先恢复受影响行为，再继续扩展功能。
